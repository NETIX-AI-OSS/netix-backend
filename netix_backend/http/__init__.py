"""HTTP client utilities; importable without Django configured."""

from netix_backend.http.clients import (
    DEFAULT_ACCEPT,
    DEFAULT_LIMITS,
    POOL_ACQUIRE_TIMEOUT,
    build_client,
    build_headers,
    reset_client_cache,
)
from netix_backend.http.health import HEALTHZ_BODY, HEALTHZ_PATH, healthz_asgi, healthz_view, send_healthz
from netix_backend.http.retry import (
    DEFAULT_BACKOFF_FACTOR,
    DEFAULT_RETRIES,
    IDEMPOTENT_METHODS,
    MAX_RETRY_AFTER_SECONDS,
    RETRYABLE_EXC,
    RETRYABLE_STATUS,
    AsyncRetryTransport,
    RetryTransport,
    RetryTransportWrapper,
    bind_django_settings,
    default_backoff,
    default_retries,
    get_async_retry_transport,
    get_retry_transport,
    interservice_httpx_args,
    is_idempotent_method,
    is_retryable_status,
    jittered_backoff,
    parse_retry_after_seconds,
    post_with_retry,
    request_with_retry,
    requests_session_with_retry,
    should_retry_status,
    unbind_django_settings,
)
from netix_backend.http.static import (
    StaticFileExpired,
    StaticFilePayload,
    StaticServiceError,
    build_static_client,
    link_or_empty,
    static_fetch,
    static_fetch_text,
    static_upload,
)

__all__ = (
    "DEFAULT_ACCEPT",
    "DEFAULT_BACKOFF_FACTOR",
    "DEFAULT_LIMITS",
    "DEFAULT_RETRIES",
    "HEALTHZ_BODY",
    "HEALTHZ_PATH",
    "IDEMPOTENT_METHODS",
    "MAX_RETRY_AFTER_SECONDS",
    "POOL_ACQUIRE_TIMEOUT",
    "RETRYABLE_EXC",
    "RETRYABLE_STATUS",
    "AsyncRetryTransport",
    "RetryTransport",
    "RetryTransportWrapper",
    "StaticFileExpired",
    "StaticFilePayload",
    "StaticServiceError",
    "bind_django_settings",
    "build_client",
    "build_headers",
    "build_static_client",
    "default_backoff",
    "default_retries",
    "get_async_retry_transport",
    "get_retry_transport",
    "healthz_asgi",
    "healthz_view",
    "interservice_httpx_args",
    "is_idempotent_method",
    "is_retryable_status",
    "jittered_backoff",
    "link_or_empty",
    "parse_retry_after_seconds",
    "post_with_retry",
    "request_with_retry",
    "requests_session_with_retry",
    "reset_client_cache",
    "send_healthz",
    "should_retry_status",
    "static_fetch",
    "static_fetch_text",
    "static_upload",
    "unbind_django_settings",
)
